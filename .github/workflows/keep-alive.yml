name: Keep-Alive Ping

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the application
        # Adding -f to fail on server errors, and --retry to handle transient blips
        run: |
          curl -f -I https://property-management-dash.streamlit.app || echo "Ping failed, but continuing..."
